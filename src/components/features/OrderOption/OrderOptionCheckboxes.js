import React from 'react';
import PropTypes from 'prop-types';

const OrderOptionCheckboxes = (props) => {
  return (
    <div>OrderOptionChockboxes</div>
  );
};

export default OrderOptionCheckboxes;